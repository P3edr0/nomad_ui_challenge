import 'package:flutter/material.dart';

const primaryColor = Color.fromRGBO(251, 211, 4, 1);
const primaryFocusColor = Color.fromRGBO(4, 115, 246, 1);

const transparent = Colors.transparent;
const secondaryColor = Colors.white;
const alertColor = Colors.red;
const secondaryFocusColor = Colors.black;
const infoColor = Colors.grey;
